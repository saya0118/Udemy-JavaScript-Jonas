// Remember, we're gonna use strict mode in all scripts now!
'use strict';
const temp =[17, 21, 23];
function printForecast(temp){
    return (`${temp[i]} in ${temp.length-1}days`);
}

printForecast(temp[2]);